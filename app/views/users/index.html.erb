<div class="container">
  <h2>Lista de traders</h2>
</div>
<div class="container">
  <div class="row">
    <% @users.each do |user| %>
    <div class="col">
      <div class="card" style="width: 30rem;">
        <div class="d-flex p-3">
          <%= gravatar_image_tag(user.email, size: 64, alt: user.username)%>
          <h5 class="card-title p-4"><%= user.username %> </h5>
        </div>
        <div class="d-flex">
          <div class="card-body">
            <h5 style="color:grey;">Trades</h5>

            <% @trades.each do |trade| %>
              <%=  %>
            <% end %>



            <a href="#" class="btn btn-primary">Go somewhere</a>
          </div>
        </div>
      </div>
      <% end %>

      </div>
    </div>
    <div class="col">

    </div>
  </div>
</div>
